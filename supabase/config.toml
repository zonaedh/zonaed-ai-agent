# Supabase CLI project config (created for zonaed-ai-agent; plan §9 Step 0)
project_id = "zonaed-ai-agent"

[api]
enabled = true
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
major_version = 17

[studio]
enabled = true
port = 54323

# RLS is enabled on all tables by the migrations; auth provider config
# (anonymous sign-in) is set in the hosted dashboard, not local config.
